# Installation
```bash
cp -r ~/.config ~/.config.backup && rm -rf ~/.config && mkdir -p ~/.config && git clone https://github.com/notVansana/dotfiles.git ~/.config && mv ~/.config/.oh-my-bash ~/.oh-my-bash && mv ~/.config/.bashrc ~/.bashrc
```
